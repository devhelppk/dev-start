# --- Database (production) ---
# REPLACE BEFORE DEPLOY: production Postgres connection string (set in platform env)
DATABASE_URL=postgresql://user:password@prod-db.example.com:5432/dev_start?schema=public

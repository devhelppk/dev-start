name: CI

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun i --frozen-lockfile
      - run: bun run lint

  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run typecheck

  build-cli:
    needs: [lint, check]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build

  verify-nextjs-base:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: CLI help works
        run: node cli/dist/index.js --help
      - name: Verify nextjs/base scaffold contract
        run: bun run verify:nextjs-base

  verify-nextjs-github-workflows:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: Verify nextjs/base + github-workflows scaffold contract
        run: bun run verify:nextjs-github-workflows

  verify-nextjs-prisma:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: Verify nextjs/base + prisma scaffold contract
        run: bun run verify:nextjs-prisma

  verify-nextjs-prisma-github-workflows:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: Verify nextjs/base + prisma + github-workflows scaffold contract
        run: bun run verify:nextjs-prisma-github-workflows

  verify-nextjs-better-auth-prisma:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: Verify nextjs/base + prisma + better-auth scaffold contract
        run: bun run verify:nextjs-better-auth-prisma

  verify-nextjs-better-auth-prisma-github-workflows:
    needs: [build-cli]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('bun.lock') }}
      - run: bun i --frozen-lockfile
      - run: bun run build
      - name: Verify nextjs/base + prisma + better-auth + github-workflows scaffold contract
        run: bun run verify:nextjs-better-auth-prisma-github-workflows
